import { myHelper } from '../../../helpers/my-helper';
import { module, test } from 'qunit';

module('Unit | Helper | my helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = myHelper(42);
  assert.ok(result);
});
