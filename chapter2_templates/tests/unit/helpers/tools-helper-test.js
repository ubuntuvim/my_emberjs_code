import { toolsHelper } from '../../../helpers/tools-helper';
import { module, test } from 'qunit';

module('Unit | Helper | tools helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toolsHelper(42);
  assert.ok(result);
});
