import { escapeHelper } from '../../../helpers/escape-helper';
import { module, test } from 'qunit';

module('Unit | Helper | escape helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = escapeHelper(42);
  assert.ok(result);
});
