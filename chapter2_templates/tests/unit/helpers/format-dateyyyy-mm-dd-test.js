import { formatDateyyyyMmDd } from '../../../helpers/format-dateyyyy-mm-dd';
import { module, test } from 'qunit';

module('Unit | Helper | format dateyyyy mm dd');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateyyyyMmDd(42);
  assert.ok(result);
});
